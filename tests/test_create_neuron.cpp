#include "boltzmann/neuron.hpp"

#include <cmath>

using namespace boltzmann;

int main(int argc, char **argv)
{
    Neuron neuron();

    //if (neuron.getName() != std::string("test"))
    //    return 1;
    //if (neuron.getId() != 0)
    //    return 2;
    //if (neuron.getLayerId() != -1)
    //    return 3;
    
    //boost::array<double, 2> neuronState = neuron.getNodeState();
    //if (std::fabs(neuronState[0]) > (double)1e-8
    // || std::fabs(neuronState[1]) > (double) 1e-8)
    //    return 4; 

    return 0;
}